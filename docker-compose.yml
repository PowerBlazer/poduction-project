version: '3.4'

services:
  react:
      container_name: production-project
      tty: true 
      build:
        context: .
        dockerfile: Dockerfile
      volumes:
        - './:/app'
      ports:
        - 3000:3000
      environment:
        - CHOKIDAR_USEPOLLING=true